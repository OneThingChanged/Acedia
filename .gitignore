# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Environment and local overrides
.env
.env.*
*.local
/app/store/store-identity.local.json

# Per-project agent config the app auto-generates (machine-local hooks)
/.codex/
/.claude/settings.local.json
/.qwen/

# Local signing keys — never commit private keys
/signing/
*.key
*.keystore
google-services.json
GoogleService-Info.plist
service-account*.json
credentials*.json
credentials*.xml
*.pfx
*.p12

# Store release private exports / copied local execution evidence
/.store-release/
/app/store/private/
**/portal-title-*.png
**/submission-before.json
**/submission-after.json
**/submission-request.json
**/submission-reconcile.json
**/release-run-*.json

# OS/editor files
.DS_Store
Thumbs.db
.idea/
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Electron experiment packaging output
/app/electron-dist/

# Local-only research and third-party comparison notes
/local-research/

# root deps / tool caches
node_modules/
/.build-tools/

# Local mobile build products (release binaries are attached separately)
/mobile/artifacts/
